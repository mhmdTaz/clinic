# Generated by the Expo CLI on `expo start`: typed-route declarations and the environment shim.
# Regenerated on every start, so they are never committed — CI typechecks without them and routes
# fall back to plain strings there.
.expo/
expo-env.d.ts
